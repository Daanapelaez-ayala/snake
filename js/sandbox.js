let lastRenderTime=0
const SNAKE_SPEED = 2
function main(currentTime){
    window.requestAnimationFrame(main)
    lastRenderTime=currentTime
    console.log(secondsSinceLastSRender)
}

window.requestAnimationFrame(main)
